#include <stdio.h>

void print_num(int i);

int main(void)

    {    
         
         print_num(10);   
         system("PAUSE");
    }
    
    void print_num(int i)
    {       
            while(1)
            {   int i=0;
                for(i;i<10;i++);      
                 printf("%d\n",n);  
            }
    }
